import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    tabMenu:{
        width: "100%",
        flex: 1,
        flexDirection: 'row',
        alignItems: 'center',
        position: 'absolute',
        bottom: 0
    },
    iconeMenu:{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        padding: 10
    }
})

export  {styles};